import type { QuizRow } from "../../quiz-types";

export const sqlLevel3AdvancedQaUseCasesQuizzes: QuizRow[] = [
  {
    lesson_title: "SQL Level 3: Advanced QA Use Cases",
    question: "What is a JOIN in SQL?",
    option_a: "A backup of a table",
    option_b: "A way to combine data from multiple tables based on a related column",
    option_c: "A type of sort operation",
    option_d: "A database compression technique",
    answer: "B",
    explanation: "JOIN combines rows from multiple tables based on a relationship (usually through foreign keys). INNER JOIN, LEFT JOIN, and RIGHT JOIN are common types.",
  },
  {
    lesson_title: "SQL Level 3: Advanced QA Use Cases",
    question: "When would a QA engineer use a LEFT JOIN in testing?",
    option_a: "Never, left joins are not useful",
    option_b: "To find data in the left table that has no matching records in the right table (orphaned records)",
    option_c: "Only for administrative purposes",
    option_d: "To delete data",
    answer: "B",
    explanation: "LEFT JOIN returns all rows from the left table plus matching rows from the right table. It's useful for finding orphaned records or missing relationships in test data.",
  },
  {
    lesson_title: "SQL Level 3: Advanced QA Use Cases",
    question: "What is an aggregate function in SQL?",
    option_a: "A function that combines tables",
    option_b: "A function that performs calculations on a set of rows (COUNT, SUM, AVG, MAX, MIN)",
    option_c: "A function that deletes data",
    option_d: "A function that sorts data",
    answer: "B",
    explanation: "Aggregate functions compute values from multiple rows. COUNT returns the number of rows, SUM totals values, AVG calculates average, MAX/MIN find extremes.",
  },
  {
    lesson_title: "SQL Level 3: Advanced QA Use Cases",
    question: "Why would a QA engineer use a GROUP BY clause?",
    option_a: "To sort the results alphabetically",
    option_b: "To group rows by a column and apply aggregate functions (like COUNT) to each group",
    option_c: "To delete rows",
    option_d: "To add new columns",
    answer: "B",
    explanation: "GROUP BY organizes rows into groups based on column values and allows aggregate functions to operate on each group. Useful for summarizing test data.",
  },
  {
    lesson_title: "SQL Level 3: Advanced QA Use Cases",
    question: "What is the purpose of a database transaction in testing?",
    option_a: "To make queries faster",
    option_b: "To group multiple SQL statements and ensure they all succeed or all fail together",
    option_c: "To backup the database",
    option_d: "To encrypt data",
    answer: "B",
    explanation: "Transactions ensure data consistency. If any statement fails, the entire transaction rolls back. This is crucial for test cleanup to maintain database integrity.",
  },
  {
    lesson_title: "SQL Level 3: Advanced QA Use Cases",
    question: "You need to verify every order has a corresponding user. What SQL would detect orphaned orders?",
    option_a: "`SELECT * FROM orders WHERE user_id IS NULL`",
    option_b: "`SELECT o.* FROM orders o LEFT JOIN users u ON o.user_id = u.id WHERE u.id IS NULL`",
    option_c: "`SELECT * FROM orders JOIN users ON orders.id = users.id`",
    option_d: "`SELECT * FROM users WHERE id NOT IN (SELECT user_id FROM orders)`",
    answer: "B",
    explanation: "LEFT JOIN keeps all orders; WHERE u.id IS NULL filters to only orders with no matching user — orphaned orders. This reveals referential integrity violations that indicate a data bug.",
  },
  {
    lesson_title: "SQL Level 3: Advanced QA Use Cases",
    question: "What is the difference between INNER JOIN and LEFT JOIN?",
    option_a: "They return the same results",
    option_b: "INNER JOIN returns only rows with matches in both tables; LEFT JOIN returns all rows from the left table plus matches from the right (NULLs where no match)",
    option_c: "LEFT JOIN is faster than INNER JOIN",
    option_d: "INNER JOIN can only join 2 tables; LEFT JOIN can join many",
    answer: "B",
    explanation: "INNER JOIN: only matched rows from both tables. LEFT JOIN: all left rows + matched right rows (NULL for unmatched). For finding missing relationships, LEFT JOIN + WHERE right.id IS NULL is the pattern.",
  },
  {
    lesson_title: "SQL Level 3: Advanced QA Use Cases",
    question: "You need to find users who placed more than 3 orders. Describe the SQL approach using GROUP BY and HAVING.",
    option_a: "`SELECT * FROM users WHERE orders > 3`",
    option_b: "`SELECT user_id, COUNT(*) as order_count FROM orders GROUP BY user_id HAVING COUNT(*) > 3`",
    option_c: "`SELECT * FROM orders LIMIT 3`",
    option_d: "`SELECT user_id FROM orders WHERE COUNT(user_id) > 3`",
    answer: "B",
    explanation: "GROUP BY user_id groups orders per user. COUNT(*) counts their orders. HAVING filters groups — only users with more than 3 orders appear. WHERE cannot use aggregate functions, so HAVING is required here.",
  },
  {
    lesson_title: "SQL Level 3: Advanced QA Use Cases",
    question: "What is the HAVING clause used for, and how does it differ from WHERE?",
    option_a: "HAVING is an older version of WHERE",
    option_b: "HAVING filters groups after aggregation; WHERE filters individual rows before aggregation",
    option_c: "HAVING is used for string comparisons; WHERE is for numbers",
    option_d: "They are interchangeable",
    answer: "B",
    explanation: "WHERE filters rows before grouping. HAVING filters the results of GROUP BY. You cannot use aggregate functions like COUNT() in a WHERE clause — that's what HAVING is for.",
  },
  {
    lesson_title: "SQL Level 3: Advanced QA Use Cases",
    question: "You're verifying that placing an order correctly adds loyalty points to the user's account (2 separate tables). How do you verify this with SQL?",
    option_a: "Check the orders table only",
    option_b: "Run a JOIN: `SELECT o.id, u.loyalty_points FROM orders o JOIN users u ON o.user_id = u.id WHERE o.id = {testOrderId}` and verify the points increased correctly",
    option_c: "Check the application UI for the points badge",
    option_d: "Ask the developer if they added the points logic",
    answer: "B",
    explanation: "JOIN the orders and users tables to verify both sides: the order was created and the user's loyalty points were updated correctly. This cross-table verification catches bugs where one operation succeeded but the other failed.",
  },
  {
    lesson_title: "SQL Level 3: Advanced QA Use Cases",
    question: "What is a subquery in SQL?",
    option_a: "A query that runs slower than normal",
    option_b: "A query nested inside another query, used to provide values or conditions to the outer query",
    option_c: "A backup query stored in the database",
    option_d: "A query with fewer than 5 columns",
    answer: "B",
    explanation: "Subquery example: `SELECT * FROM users WHERE id IN (SELECT user_id FROM orders WHERE total > 1000)` — the inner query finds high-value order user IDs; the outer query returns those users.",
  },
  {
    lesson_title: "SQL Level 3: Advanced QA Use Cases",
    question: "After a data migration test, 5 users have NULL in their email column. What SQL would you write to find and count them?",
    option_a: "`SELECT * FROM users WHERE email = NULL`",
    option_b: "`SELECT COUNT(*) FROM users WHERE email IS NULL` to count, then `SELECT * FROM users WHERE email IS NULL` to inspect them",
    option_c: "`SELECT * FROM users WHERE email LIKE 'NULL'`",
    option_d: "`SELECT * FROM users WHERE email = ''`",
    answer: "B",
    explanation: "NULL comparisons must use IS NULL, not = NULL (which always returns false). First count to confirm the scope, then inspect the specific records to understand which migration step failed.",
  },
  {
    lesson_title: "SQL Level 3: Advanced QA Use Cases",
    question: "What is a database VIEW and how might a QA engineer use one?",
    option_a: "A saved screenshot of database results",
    option_b: "A named saved query that acts like a virtual table — QA can use predefined views to quickly access complex joined data without writing the JOIN every time",
    option_c: "A way to display database schema in a GUI",
    option_d: "A read-only copy of a table",
    answer: "B",
    explanation: "A view encapsulates a complex query (e.g., orders_with_users that joins orders and users). QA can query `SELECT * FROM orders_with_users WHERE status='pending'` without writing the JOIN each time.",
  },
  {
    lesson_title: "SQL Level 3: Advanced QA Use Cases",
    question: "After a test suite run, you want to clean up all test data where username starts with 'test_'. What SQL would you write?",
    option_a: "`DELETE FROM users WHERE username = 'test_'`",
    option_b: "`DELETE FROM users WHERE username LIKE 'test_%'`",
    option_c: "`DROP TABLE users WHERE username LIKE 'test_%'`",
    option_d: "`UPDATE users SET deleted = true WHERE username LIKE 'test_%'`",
    answer: "B",
    explanation: "`LIKE 'test_%'` matches any username starting with 'test_' followed by one or more characters. The `%` wildcard handles any suffix. Always run the SELECT equivalent first to preview what will be deleted.",
  },
  {
    lesson_title: "SQL Level 3: Advanced QA Use Cases",
    question: "What does ROLLBACK do in a SQL transaction and why is it useful during testing?",
    option_a: "It re-runs the last SQL statement",
    option_b: "It undoes all changes made within the current transaction, returning the database to its state before the transaction began",
    option_c: "It saves the transaction for later",
    option_d: "It creates a backup of the database",
    answer: "B",
    explanation: "ROLLBACK is invaluable for test cleanup: BEGIN TRANSACTION → run test → ROLLBACK returns DB to clean state without any residual data. This is faster than explicit DELETE cleanup and guarantees a clean state.",
  },
];
